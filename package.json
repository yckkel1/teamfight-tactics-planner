{
  "name": "teamfight-tactics-planner",
  "private": true,
  "type": "commonjs",
  "prisma": { "seed": "node prisma/seed.js" },
  "scripts": {
    "dev": "pnpm -F @data/models run build && pnpm -F @app/api run dev",
    "build": "pnpm -F @data/models run build && pnpm -F @app/api run build",
    "start": "pnpm -F @app/api run start",
    "db:reset": "rm -f prisma/.data/tft.db",
    "db:all": "pnpm exec prisma generate --schema prisma/schema.prisma && pnpm exec prisma migrate deploy --schema prisma/schema.prisma && pnpm exec prisma db seed --schema prisma/schema.prisma && node prisma/seed.tiers.js && node prisma/import.units.js && node prisma/validate.tiers.js && node prisma/validate.units.js"
  },
  "devDependencies": {
    "prisma": "^6.15.0",
    "zod": "^3.25.76"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0"
  }
}